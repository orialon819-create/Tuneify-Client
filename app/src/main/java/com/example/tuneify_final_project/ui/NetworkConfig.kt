package com.example.tuneify_final_project.ui

object NetworkConfig {
    // This is your 'Sticky Note'
    // We use 'var' so we can change it when the user types a new one
    var serverIp: String = "192.168.1.30"
    val serverPort: Int = 5000

    val streamPort: Int = 8000
}